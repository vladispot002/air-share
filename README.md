# LAN-Drop
### Cross-Platform P2P & Hybrid Cloud Data Transfer System

A cross-platform system for file sharing between devices in local area networks (LAN/Wi-Fi) and via external cloud services.

### Project Purpose
Enabling data transfer across Linux, Windows, Android, and iOS operating systems without requiring an internet connection. The system supports automatic download resumption after connection loss and chunk-based file integrity verification.
